# Current State

- Repository: `C:\product\Quick Processing Tool` (`main`). v0.4.0 is published. The v0.4.1 image-merge hotfix is uncommitted and awaiting human acceptance; no v0.4.1 tag, push, or GitHub Release exists.
- The committed application implements seven Japanese-first tools: Quick conversion (including crop and split), image editing, sound-effect and speech-bubble material makers, upscaling, pixel editing, and thumbnail generation.
- The v0.3.0 release changes are split into `fix: guard upscale queue selection` and `chore: prepare v0.3.0 release`. The latter synchronizes version/documentation/Windows metadata and improves optional-Runtime guidance.
- The formal release environment is an isolated 64-bit CPython 3.12.13 venv with the exact `constraints-release.txt` dependencies. The pre-existing CPython 3.12.14 development venv remains unchanged.
- A fresh v0.3.0 package has been built from the committed RC. The formal icon, package version information, runtime exclusion, single-EXE/development-payload audit, and setup-script inclusion pass.
- The optional User-scope Real-ESRGAN Runtime is available. Fresh-package code-path smoke passed: real 2x upscale, four-way split save, and pixel-identical rejoin. The fresh EXE started, created its main window, and closed normally.
- Committed preview work adds shared Fit / 100% / 200% / 400% controls to Quick split and Image Edit, zoom-safe hand drawing and eyedropping, middle-button pan, and fixed-zoom resize center retention. Focused Image Edit and Quick split tests pass; live GUI automation is currently unavailable because the Windows helper fails during setup refresh.

- Committed Phase 5B work adds an always-available header action that opens a standalone metadata-removal batch dialog. It accepts image files and direct folder contents, writes collision-safe copies by default to each source's Processed folder, strips privacy/generation metadata while retaining ICC profiles, alpha, displayed orientation, and animation timing, and isolates per-file failures. Focused tests, full regression, and offscreen GUI launch pass; live GUI automation remains unavailable because the Windows helper fails during setup refresh.
- Committed Phase 5C work adds non-destructive color adjustments (brightness, contrast, saturation, temperature, tint, hue, optional fade) and final-image horizontal flip to Image Edit. Controls are compact Slider + numeric pairs with color-only reset; adjustments run before transparency/palette and preserve alpha, while final flip includes mosaic/hand layers. Focused editing/UI regression, full pytest, compileall, diff-check, and offscreen GUI smoke pass.
- Committed WebP verification fix distinguishes actual transparency from the renderer's always-RGBA working mode. Opaque WebP exports now pass strict format/size/reopen verification, while true alpha loss still fails; user-visible verification messages now identify reopening, format, dimensions, or transparency failures.
- Committed Phase 5D work adds Quick conversion image merge: an ordered dedicated 2–6 image selection, horizontal/vertical layout, original or aspect-preserving matched dimensions, alignment, 0–100 px gap, white/black/transparent gap backgrounds, in-memory PreviewCanvas rendering, and one collision-safe output through existing destination/format semantics. PNG/WebP preserve alpha while JPEG uses the established background flattening. Gap changes are coalesced through a 24ms single-shot timer after QSpinBox has delivered a valid integer, avoiding native SpinBox re-entrancy while retaining immediate preview updates. Focused tests, related Quick regression, full pytest, compileall, diff-check, and offscreen startup pass; live GUI acceptance remains pending because the Windows helper fails during setup refresh.
- Committed Phase 5E work extends Quick image merge with row-major 2/3-column grids for up to 9 inputs. It reuses the ordered merge queue, gap/background/alpha/export behavior and in-memory preview; original, aspect-preserving width/height matching, and non-distorting cell fit are available. Empty final cells use the selected background. The merge queue supports Qt internal drag-and-drop reordering: the completed Drop updates the `merge_paths` model, renumbers the list, and schedules one preview update, so Preview and export retain the same order. The Quick accordion order is image merge, split, crop, resize, rotate/flip, format, capacity reduction, then destination/privacy. Focused tests (50), full pytest, compileall, diff-check, and offscreen checks at 900×620 and 1180×720 pass; Windows Computer Use remains unavailable for automated live interaction.
- Committed Phase 5E finish work allows a -100〜100 px Gap for horizontal/vertical image merge. Negative values overlap adjacent images; later queue entries are composited in front, and an image advances by at least 16 px (or its full primary dimension when smaller) so canvas dimensions stay valid. Grid keeps its 0〜100 px contract until a separate overlap layout is specified. Focused tests (64), full pytest, compileall, diff-check, and offscreen Gap UI checks pass.
